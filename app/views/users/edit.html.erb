<!DOCTYPE html>
<html>
<head>
  <title>Verify your Email</title>
  <style>
    /* Add CSS styles for layout and appearance */
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5; /* Light white background */
      color: #000; /* Black text */
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .header {
      background-color: #000; /* Black background for titles */
      color: #fff; /* White text for titles */
      text-align: center;
      padding: 40px 0;
      font-size: 36px;
    }
    .content {
      width: 40%; /* Adjust the width as needed (e.g., 80%) */
      margin: 0 auto;
      padding: 20px;
      background-color: #fff; /* White background for content */
      color: #000; /* Black text for content */
      border-radius: 5px;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
      text-align: left; /* Center content horizontally */
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      margin: 10px 0;
    }
    a {
      color: #007BFF; /* You can choose to keep links with a different color */
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    .footer {
      text-align: center;
      padding: 20px;
      background-color: #000; /* Black background for the footer */
      color: #fff; /* White text for the footer */
    }
  </style>
</head>
<body>
  <div class="header">
    User Lists
  </div>

  <div class="content">
    <h1>Edit Account <%= @user.firstname %> <%= @user.secondname %></h1>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>

    <%= form_with model: @user do |form| %>
      <div>
        <%= form.label :Validate_previous_email %><br>
        <%= form.text_field :validate_email %>
      </div>

      <div>
        <%= form.label :FirstName %><br>
        <%= form.text_field :firstname %>
        <% @user.errors.full_messages_for(:firstname).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>

      <div>
        <%= form.label :LastName %><br>
        <%= form.text_field :secondname %>
        <% @user.errors.full_messages_for(:secondname).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>

      <div>
        <%= form.label :New_Email %><br>
        <%= form.text_field :email %>
        <% @user.errors.full_messages_for(:email).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>

      <div>
        <%= form.submit %>
      </div>

      <div>
        <%= link_to 'Back to User Lists', users_path, class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>

  <div class="footer">
    KAI Team - Team Members: A, B
  </div>
</body>
</html>
